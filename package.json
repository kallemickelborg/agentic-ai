{
  "name": "agentic-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "pip3 install -r requirements.txt && cp -r backend netlify/functions/",
    "build:all": "npm run build:frontend && npm run build:backend"
  }
}
